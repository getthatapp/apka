
<%= form_for [@board, @list] do |f| %>
	<% if @list.errors.present? %>
		<ul>
			<% @list.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	<% end %>
	<%= f.text_field :title %>
	<%= f.submit %>
<% end %>